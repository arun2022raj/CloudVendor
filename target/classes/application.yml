spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vendor_cloud
    username: root
    password: root@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql:true
server:
  port: 8081

 
 
  
